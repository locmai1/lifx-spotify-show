extends default

block content 
  div
      h1= 'can\'t find LIFX device(s)'

      div(class='media')
        img(src=user.images[0].url)

      br

      a(href='/user')='Retry'
      a(href='/logout')='Logout'
